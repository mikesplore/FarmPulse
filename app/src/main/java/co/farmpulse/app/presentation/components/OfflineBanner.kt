package co.farmpulse.app.presentation.components

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun OfflineBanner(cachedAt: Long) {
    Text("Offline — last updated placeholder")
}

